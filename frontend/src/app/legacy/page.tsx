import { CustomerIntelligencePage } from "@/features/customer-intelligence/CustomerIntelligencePage";

/** 기존 Working Demo 화면. 새 화면으로 교체한 뒤에도 검증용으로 유지한다. */
export default function Legacy() {
  return <CustomerIntelligencePage />;
}
